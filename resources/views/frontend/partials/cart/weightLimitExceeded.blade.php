<div class="text-center p-4">
    <i class="las la-exclamation-triangle la-3x text-danger mb-3"></i>
    <h4 class="fw-700">{{ translate('Weight Limit Exceeded') }}</h4>
    <p class="fs-14">
        {{ translate('You cannot add more than') }}
        <strong>{{ $limit }} KG</strong>
        {{ translate('in the cart.') }}
    </p>
</div>
